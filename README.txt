
CircuMet Extended Package
-------------------------
Contains synthetic datasets and trained scikit-learn models (joblib) plus a Streamlit app.
Run:
pip install -r requirements.txt
streamlit run app.py
